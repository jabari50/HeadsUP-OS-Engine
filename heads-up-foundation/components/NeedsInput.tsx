/** Clearly-marked placeholder for content Jabari must confirm — never ship as-is. */
export default function NeedsInput({ label }: { label: string }) {
  return <span className="needs-input">[[NEEDS JABARI INPUT: {label}]]</span>;
}
