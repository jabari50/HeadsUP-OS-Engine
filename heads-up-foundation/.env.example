# Supabase — create a dedicated project for the Foundation site, then fill these in.
# Public (safe for client):
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side ONLY — never expose to the client. Used by /api/intake.
SUPABASE_SERVICE_ROLE_KEY=
